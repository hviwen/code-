# Practice Code

Interview and handwritten practice code lives here.

## Boundary

Use `practice/` for first-pass hand-written answers, retries, and timed drills.
Move only stable, reusable implementations to `src/`.

- `challenge/`: staged challenge exercises.
- `javascript/`: JavaScript practice problems, review code, and playground files.
