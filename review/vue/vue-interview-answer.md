# Vue.js 面试题集（2025年版）

> 本题库包含 **140 道** Vue.js 面试题，按难度分为简单、中等、深入三个级别。  
> 适用于 2025 年国内主流互联网公司前端面试准备。  
> 本文档仅包含问题，不提供答案。

---

## 📋 题目分布

| 难度级别 | 题目数量 | 考察重点 |
|---------|---------|---------|
| 🟢 简单 | 55 题 | 基础概念、API 使用、常见用法 |
| 🟡 中等 | 60 题 | 原理理解、常见场景、实际应用 |
| 🔴 深入 | 25 题 | 源码实现、性能优化、架构设计 |

---

## 🟢 简单级别（55题）

### Vue 基础概念（1-15）

**1.** Vue 2 和 Vue 3 的主要区别有哪些？

1. vue2 和 vue3的底层实现方式不同，vue2为兼顾老版本浏览器采用 Object.definePrototype 的方式对数据进行geter/setter ,需要使用 vue.set的方式来更新数据。相比Vue3 采用ES6中的Proxy对数据做响应式，通过track和tigger来对数据的读取，写入，删除，不存在数据访问等做监听完成响应式操作。
2. 生命周期不同，在vue2版本中 有beforeCreate和create 和 destroy等生命周期，在vue3中使用 setup 和 onUnMounted代替
3. vue3新增了组合式API 选项式API的写法和组合式函数
4. Vue3 支持typescript
5. vue3 新增了Teleport Suspense Transiion等内置组件
6. vue3 新增tree-Sharking 更小的包体积和运行速度
7. vue3 支持多根节点
8. vue3 新增v-model 数据双向绑定

**2.** 什么是 `ref`，与 `reactive` 的区别是什么？

1. ref是vue中可以定义响应式数据的函数。通常用于将原始类型数据转为响应式对象。在javascript中可以通过访问响应式对象的.value 访问到其真值。也可以传入非原始类型数据，但是在其内部使用reactive来对非原始类型数据进行包装。在模版中可以自动解包。
2. reactive同样是用于定义响应式数据的函数，但与ref不同的是他用于包装非原始类型数据。返回对象同样具有响应式。可以直接通过对象访问其真实值。

**3.** 如何创建一个计算属性（computed）？它与普通函数有什么区别？

1. 计算属性默认通过一个getter()函数来根据内部响应式值的变化自动更新其结果，返回一个ref类型的值。也可以为其设置get/set函数。set函数可以改变对应的响应式数据
2. 与普通函数不同的是：
   1. 计算属性可以缓存计算值，在响应式数据没有发生改变时其计算结果不改变且不用重复计算。
   2. 计算属性只能是同步的，不可以执行异步操作
   3. 计算属性根据响应式值的变化自动计算结果并返回一个ref类型的值

**4.** `setup()` 的执行时机是什么？能访问 `this` 吗？

1. setup() 执行在初始化构建期间，此时DOM还没有构建，props刚传入。这个期间在执行数据的初始化状态设置。
2. 不能访问this，因为dom还没有构建

**5.** 如何在 `<script setup>` 中定义 props 和 emits？

1. defineProps() 宏
2. defineEmits() 宏

**6.** `v-if` 与 `v-show` 的区别是什么？各自适用什么场景？

1. v-if和v-show都是vue中的指令
2. v-if 在于判断组件/标签是否存在，如果为false则不做任何事情，为true则按照正常规则创建。切换v-if的值可以造成对应组件/标签的销毁和重建，频繁切换开销比较大
3. v-show 只控制对应组件/标签块的css中的display是否为none ，切换不会操成组件的销毁和重建。更适合需要频繁切换的场景

**7.** `v-for` 上为什么需要 `key`？如何选择合适的 key？

1. v-for 使用key，可以有效绑定子组件的身份，用于diff算法时只对有变更的key做更新。
2. key需要绑定唯一的值。可以是string 和 number类型

**8.** Vue 中的单向数据流是什么意思？

1. 单向数据流是指子组件的数据渲染只来自父组件的传递，而不可以直接修改props数据。当需要做数据变更时，需要通过事件触发父组件，通过更新数据源重新渲染新的值。

**9.** 什么是 Vue 组件？如何定义一个组件？

1. 将公共的可以复用的部分抽离，通过接收props，emits方式来实现一个独立的导出的结构可以被调用的组件。
2. 可以通过 defineOption

**10.** 什么是 SFC（单文件组件）？它有什么优势？
1. single file component。就是将视图部分通过 template 包含的xml标签组件组合，逻辑部分通过在script 标签内js实现。样式部分通过在style标签内通过css样式实现。
2. 优势是关注点更聚焦

**11.** `v-bind` 和 `v-on` 的作用分别是什么？有什么简写形式？
1. v-bind 绑定动态属性 简写 :
2. v-on 绑定事件 简写 @

**12.** 什么是模板引用（template refs）？如何使用？
1. 模版引用，是用ref访问一个组件的全部实例和方法
2. 可以通过
   ```vue
   const childRef = ref(null)
   <Child ref="childRef" />

   或者
   const childRef = useTemplate()
   ```

**13.** Vue 3 中的 `defineProps` 和 `defineEmits` 是什么？
1. defineProps 宏是用来定义组件属性
2. defineEmits 宏是用来定义事件
3. 都只在组合式API下使用

**14.** 什么是 `v-model`？在 Vue 3 中有什么变化？
1. v-model 是vue3中的数据双向绑定，本质上是 v-bind:'valueModel' 和 v-on:'update:valueModel'的简写
2. 简化了数据传递和开发的心智

**15.** Vue 中如何实现条件渲染？有哪些方式？
1. 通过v-if判断
2. component is

### 组件通信（16-25）

**16.** 父组件如何向子组件传递数据？
1. 通过props属性值传递

**17.** 子组件如何向父组件传递数据？
1. 通过emit事件响应 向父组件传递数据

**18.** 什么是 `provide` / `inject`？有什么使用场景？
1. vue中祖孙组件传递数据的方式，provide 提供，inject 注入。成对使用
2. 可以通过在祖父组件中 provide(key,xxx) 提供,在任意子孙组件中通过const xxx = inject(key)接收

**19.** Vue 中有哪些组件通信方式？请列举并简述。

**20.** 如何在兄弟组件之间通信？

**21.** `$attrs` 和 `$listeners` 的作用是什么？（Vue 3 中有何变化？）

**22.** 什么是透传 Attributes（Fallthrough Attributes）？

**23.** 如何在组件中访问父组件或子组件实例？

**24.** 事件总线（Event Bus）的原理是什么？Vue 3 中推荐用什么替代？

**25.** `v-model` 在自定义组件中是如何工作的？

### 生命周期（26-32）

**26.** Vue 3 组件的生命周期钩子有哪些？按顺序列出。

**27.** `onMounted` 和 `onCreated`（beforeCreate/created）有什么区别？

**28.** 什么时候应该使用 `onBeforeUnmount` 和 `onUnmounted`？

**29.** `<script setup>` 中如何使用生命周期钩子？

**30.** 父子组件的生命周期钩子执行顺序是怎样的？

**31.** 在哪个生命周期钩子中可以访问 DOM？

**32.** `onActivated` 和 `onDeactivated` 是什么？什么场景下使用？

### 指令与模板语法（33-40）

**33.** 如何创建一个自定义指令（directive）？举例说明。

**34.** 自定义指令的生命周期钩子有哪些？

**35.** 什么是修饰符（Modifier）？举例说明常用的事件修饰符。

**36.** 动态组件是什么？如何使用 `<component :is="...">`？

**37.** 什么是插槽（Slot）？有哪些类型的插槽？

**38.** 如何使用具名插槽和作用域插槽？

**39.** `v-once` 和 `v-memo` 的作用是什么？

**40.** 如何在模板中使用 JavaScript 表达式？有什么限制？

### Vue 3 新特性（41-55）

**41.** 什么是 Composition API？与 Options API 有什么区别？

**42.** `<script setup>` 语法糖有什么优势？

**43.** `watch` 和 `watchEffect` 的区别是什么？

**44.** 什么是 `shallowRef` 和 `shallowReactive`？什么场景下使用？

**45.** 如何使用 `toRef` 和 `toRefs`？它们解决什么问题？

**46.** `Teleport` 组件的用途是什么？如何使用？

**47.** `Suspense` 组件的基本作用是什么？

**48.** 什么是 `defineExpose`？为什么需要它？

**49.** `nextTick` 有什么用途？什么时候使用？

**50.** Vue 3 中移除了哪些 Vue 2 的特性或 API？

**51.** 什么是 `Fragment`（片段）？Vue 3 中有什么变化？

**52.** `readonly` 的作用是什么？与 `const` 有什么区别？

**53.** 如何在 Vue 3 中使用 TypeScript？

**54.** 什么是 `isRef`、`unref`、`toRaw`？分别用于什么场景？

**55.** Vue 3 的 `emits` 选项有什么作用？为什么推荐声明 emits？

---

## 🟡 中等级别（60题）

### 响应式原理（56-70）

**56.** Vue 3 响应式系统是如何实现的？与 Vue 2 有什么区别？

**57.** `Proxy` 相比 `Object.defineProperty` 有什么优势？

**58.** 解释 Vue 3 响应式系统中 `track` 与 `trigger` 的作用。

**59.** 为什么 `reactive` 不能用于基本类型？

**60.** `computed` 的缓存机制是如何工作的？缓存什么时候失效？

**61.** 响应式对象解构后为什么会丢失响应性？如何解决？

**62.** `ref` 在模板中为什么可以不写 `.value`？

**63.** 什么是依赖收集？Vue 是如何进行依赖收集的？

**64.** 如何防止响应式对象被意外修改？

**65.** `triggerRef` 的作用是什么？什么场景下需要使用？

**66.** Vue 3 如何实现数组的响应式？与 Vue 2 有什么区别？

**67.** 什么是副作用函数（Effect）？在 Vue 中如何体现？

**68.** 响应式系统中的 `targetMap` 数据结构是什么样的？

**69.** 为什么修改响应式对象的属性后，视图不是立即更新的？

**70.** `effectScope` 的作用是什么？如何使用？

### Composition API 深入（71-85）

**71.** 如何实现一个可复用的 `useFetch` composable？要考虑哪些边界情况？

**72.** 如何实现防抖/节流的 composable？要注意什么依赖问题？

**73.** 如何在 composable 中正确处理组件卸载时的清理工作？

**74.** composable 与 mixin 的区别是什么？composable 有什么优势？

**75.** 如何让 composable 支持 SSR（服务端渲染）？

**76.** 如何实现一个 `useLocalStorage` composable？

**77.** 如何在 composable 中使用 `provide` / `inject`？

**78.** `getCurrentInstance` 的作用是什么？应该什么时候使用它？

**79.** 如何在 composable 中使用生命周期钩子？

**80.** 什么是 `onScopeDispose`？与 `onUnmounted` 有什么区别？

**81.** 如何实现一个响应式的 `useWindowSize` composable？

**82.** composable 函数的命名约定是什么？为什么推荐用 `use` 前缀？

**83.** 如何测试 composable 函数？

**84.** 如何在 composable 之间共享状态？

**85.** 如何让 composable 接受响应式参数和非响应式参数？

### 状态管理（86-95）

**86.** Pinia 与 Vuex 的区别是什么？为什么 Vue 3 推荐使用 Pinia？

**87.** Pinia store 的三个核心概念是什么？如何定义和使用？

**88.** 如何在 Pinia 中实现模块化？

**89.** `storeToRefs` 的作用是什么？为什么需要它？

**90.** Pinia 中的 `$reset`、`$patch`、`$subscribe` 分别有什么作用？

**91.** 如何在 Pinia action 中访问其他 store？

**92.** Pinia 插件系统是如何工作的？如何编写自定义插件？

**93.** 如何实现 Pinia 状态的持久化？

**94.** 在组件外（如路由守卫中）如何使用 Pinia store？

**95.** 什么时候应该使用全局状态管理，什么时候使用组件本地状态？

### Vue Router（96-105）

**96.** Vue Router 的基本使用步骤是什么？

**97.** 路由守卫有哪些类型？分别在什么时机触发？

**98.** 如何实现基于角色的路由权限控制？

**99.** 路由懒加载是什么？如何实现？有什么好处？

**100.** `<router-view>` 和 `<router-link>` 的作用是什么？

**101.** 编程式导航和声明式导航的区别是什么？

**102.** 动态路由和嵌套路由的区别是什么？如何使用？

**103.** 路由元信息（meta）的作用是什么？如何使用？

**104.** 如何处理 404 页面？如何实现路由重定向？

**105.** Vue Router 4 相比 Vue Router 3 有哪些变化？

### 性能优化（106-115）

**106.** 如何避免大型列表渲染的性能问题？

**107.** 什么是虚拟滚动？如何实现？

**108.** `v-memo` 指令的作用是什么？如何使用？

**109.** `keep-alive` 的作用是什么？有哪些属性？

**110.** 如何使用 `shallowRef` / `shallowReactive` 优化性能？

**111.** 如何使用 `markRaw` 避免不必要的响应式代理？

**112.** 组件懒加载有哪些方式？异步组件如何定义？

**113.** 如何分析和优化 Vue 应用的首屏加载时间？

**114.** Vue 应用中常见的内存泄漏场景有哪些？如何避免？

**115.** 如何使用 Vue Devtools 进行性能分析？

---

## 🔴 深入级别（25题）

### 源码与原理（116-130）

**116.** 解释 Vue 3 响应式系统的核心数据结构（effect、dep、targetMap）。

**117.** 描述虚拟 DOM（VNode）在 Vue 中的完整生命周期，从创建到 patch。

**118.** Vue 的调度器（scheduler）是如何工作的？为什么 `nextTick` 是异步的？

**119.** Vue 编译器是如何将模板转换为渲染函数的？（简述 parse → transform → codegen）

**120.** Vue 的 diff 算法是如何工作的？与 React 的 diff 有什么区别？

**121.** `render` 函数与 JSX 的应用场景及优缺点分别是什么？

**122.** Vue 3 的静态提升（Static Hoisting）优化是什么原理？

**123.** 什么是 Block Tree？Vue 3 是如何通过它优化更新性能的？

**124.** `h()` 函数的作用是什么？它与 `createElement` 有什么关系？

**125.** Vue 的 Patch Flags 是什么？它如何优化 diff 过程？

**126.** Pinia store 的内部实现原理是什么？state 是如何响应化的？

**127.** Vue Router 的路由匹配（matcher）是如何实现的？

**128.** `Suspense` 组件的异步处理机制是如何实现的？

**129.** Vue 的依赖注入系统（provide/inject）是如何跨组件传递的？

**130.** 自定义指令的底层实现原理是什么？

### 架构与工程化（131-140）

**131.** 如何设计和实现一个完整的前端权限管理系统？

**132.** 在微前端架构中，Vue 应用应该如何集成？有哪些方案？

**133.** 如何在 Vue 应用中实现主题切换和暗黑模式？

**134.** Vue 应用的国际化（i18n）完整实现方案是什么？

**135.** 如何设计和实现复杂的动态表单系统？

**136.** Vite 与 Webpack 在 Vue 项目中的选择标准是什么？

**137.** 如何为 Vue 项目设计合理的代码分割策略？

**138.** Monorepo 架构中如何管理多个 Vue 项目？

**139.** Vue 项目的 CI/CD 流程应该如何设计？

**140.** 如何在 Vue 项目中实现错误监控和异常处理？

---

## 📚 补充知识点题目

以下题目涵盖测试、TypeScript 和实际项目场景，供进阶学习使用。

### 测试相关（补充 10 题）

**T1.** 如何使用 Vue Test Utils 测试组件的 props 传递和验证？

**T2.** 如何测试包含异步操作（API调用）的 Vue 组件？

**T3.** 如何 mock Pinia store 进行组件单元测试？

**T4.** 如何对 Vue Router 的路由守卫进行单元测试？

**T5.** 如何测试自定义指令和 composables 函数？

**T6.** Vue 应用的 E2E 测试应该如何设计？推荐使用哪些工具？

**T7.** 如何测试 Vue 组件的可访问性（a11y）？

**T8.** 如何测试 Vue 组件中的用户交互（点击、输入等）？

**T9.** 快照测试在 Vue 项目中如何使用？有什么优缺点？

**T10.** 在 Vue 项目中如何实现测试驱动开发（TDD）？

### TypeScript 集成（补充 10 题）

**TS1.** 如何在 Vue 3 + TypeScript 项目中正确定义组件的 props 类型？

**TS2.** 如何为 Pinia store 添加完整的 TypeScript 类型支持？

**TS3.** Vue Router 4 中如何实现类型安全的路由参数？

**TS4.** 如何在 Vue 3 中定义和使用泛型组件？

**TS5.** 如何处理第三方 Vue 组件库的 TypeScript 类型定义问题？

**TS6.** Vue 3 Composition API 中的类型推断机制是如何工作的？

**TS7.** 如何为 Vue 组件的 emit 事件定义精确的 TypeScript 类型？

**TS8.** `defineProps<T>()` 与 `defineProps({})` 有什么区别？

**TS9.** 如何在 Vue 3 中使用 `PropType` 定义复杂类型？

**TS10.** 如何为模板引用（ref）添加正确的类型声明？

### 实际项目场景（补充 10 题）

**P1.** 如何在 Vue 应用中实现大文件上传和断点续传功能？

**P2.** Vue 应用中实时通信（WebSocket）的集成方案是什么？

**P3.** 如何在 Vue 应用中实现数据埋点和用户行为分析？

**P4.** 如何实现 Vue 应用的离线功能和 PWA 特性？

**P5.** 如何在 Vue 应用中集成图表库（ECharts、Chart.js等）？

**P6.** Vue 项目中富文本编辑器的选择和集成方案有哪些？

**P7.** 如何在 Vue 应用中实现数据可视化大屏？

**P8.** Vue 应用的移动端适配策略是什么？如何实现响应式设计？

**P9.** 如何选择和集成 Vue UI 组件库？有哪些定制化策略？

**P10.** Vue 项目中第三方 SDK 的集成策略是什么？

---

## 📖 使用建议

### 学习路径

1. **初级开发者**：先掌握简单级别全部题目（1-55）
2. **中级开发者**：在简单级别基础上，重点攻克中等级别（56-115）
3. **高级开发者**：深入学习深入级别（116-140）+ 补充题目

### 面试准备策略

| 目标岗位 | 重点题目 | 建议时间 |
|---------|---------|---------|
| 初级前端 | 简单全部 + 中等 50% | 1-2 周 |
| 中级前端 | 简单 + 中等全部 + 深入 30% | 2-3 周 |
| 高级/专家 | 全部题目 + 源码阅读 | 3-4 周 |

### 复习技巧

- 每道题尝试用自己的话表述，确保真正理解
- 对于原理类问题，尝试手写简化实现
- 结合实际项目经验准备案例
- 关注 Vue 官方文档的更新和 RFC

---

> **版本说明**：本题库基于 Vue 3.x 设计，包含部分 Vue 2 对比内容。
> **最后更新**：2025年11月

