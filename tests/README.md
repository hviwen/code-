# Tests

Runnable test specs live here. Current coverage is focused on `useFetch`.

Run tests with:

```sh
pnpm test
```

The test command uses Node's built-in test runner with TypeScript type stripping.
